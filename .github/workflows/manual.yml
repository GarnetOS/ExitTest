on:
  workflow_dispatch:
name: Test
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Disk
        run: |
            df -h
            sudo du -a / | sort -n -r | head -n 20
            sudo apt-get purge -y dotnet-sdk-3.1 dotnet-sdk-2.1
            dotnet --list-sdks
