#!/bin/bash
set -e
export MAKEFLAGS='-j2'
cd $LFS/sources
ls -l /usr/bin/sudo
sudo chown -R root:root $LFS/{usr,lib,var,etc,bin,sbin,tools}
case $(uname -m) in
  *64) sudo chown -R root:root $LFS/lib64 ;;
esac
